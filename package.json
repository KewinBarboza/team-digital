{
  "name": "team-digital",
  "version": "0.0.1",
  "private": true,
  "homepage": "http://teamdigital.dev",
  "scripts": {
    "predeploy": "npm run build && echo teamdigital.dev > ./dist/CNAME",
    "deploy": "gh-pages -d dist",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^1.0.0-beta.72"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}
